# 2026-09-21 工作日志（B18 收尾批：P1→P5）

## 会话背景

用户 09:26 派发「读取 docs/parallel-batches-w5/b18-remaining-impl-2026-09-20.md」，
口头确认「不要在 zcode 上实施，就通过 workbuddy ai 上实施」，并追加
「完成 b18 就结束」（= 做完 P1→P5，不进 B20/B19）。

前置实况：原 P1 会话（ZCode）自 09-21 00:07 起停滞（环境 ACL/AV 封锁所致），
工作区留有 **A1 全套未提交**（6 改 + 1 新增）。本会话承接续作。

## B18-P1-A1 完成并入库（commit 85603146a）

- proto：TypedValue/TypedField/TypedRow 通用递归承载 + `CollectionChange.upsertsTyped=4`
  + `JsonFieldChange.valueTyped=3`（旧 2 号 bytes 停写保留、号冻结）；
  TypedValue 含 `vNull` 防御位 + **`vEmptyArray` 判别位**（b02 发现 7 纪律）。
- C++：`gameview_encode.cpp` 新增 `appendTypedValue`/`appendTypedRow` 递归助手，
  删两处 bytes 编码分支。
- Kotlin：`GameViewMirrorCodec` 两处单点切读（typed 优先 + 旧 bytes fallback 保留）。
- 守卫三层：C++ 编码层 3 用例（含空容器判别）+ 新增
  `MirrorTypedEnvelopeEquivalenceTest`（3 例：JVM 双路 + C++ 真编码臂 + 数字 content 红线）
  + 端到端原断言零改动。
- 修正前会话遗留的 3 处缺陷（均实测打回）：缺 `toJsonElement` import（编译红）、
  `envelope` NestedBlockDepth 4（detekt 红）、TypedField 子消息当 key 比对（ctest 红）；
  另加固两处：C++ 臂显式 `discipleRowsAsPatches=false`（防形态缺省短路）、
  JVM 臂字符串分支优先（防数字形字符串静默分叉）。
- 门禁：桥重建 EXIT=0；**ctest 1557/1557 EXIT=0**；组合门 **BUILD SUCCESSFUL 19m35s**
  （339 任务 / 34 executed）；六模块 7879 例 0 失败 17 既有跳过；Diff* 273 例 0 skip；
  Canvas 兜底 106 例 0 skip；JNI 87/87。

## 重要基线校正（写回 skill）

**ctest 真实基线 = 1556**（历史文档/skill 记的 1553 是**单进程直跑**口径误记）。
本轮 A1 新增 1 例 ⇒ 1557。以后按 1556 计增。

## A2 实施中（storageBagItemsTyped）

关键设计发现（超出施工卡口径，已按纪律补足）：
**75 号 bytes 是标量**（presence 天然区分"未携带"与"携带空袋"）；换轨到
repeated `storageBagItemsTyped` 后**零条目同时表示两种相反语义**（列缺省 /
列脏且清空）⇒ 列级合并（`mergeToDisciple` 的 REPEATED_FIELD_CLEARERS）会让
"清空袋"静默丢失。实测该路径可达：`auto_gear.h:936`、`month_settlement.h:1698`
均可在袋被扣空后标脏该列。
处置 = 追加 `optional bool storageBagItemsPresent = 111` 列携带位（C++ 行 JSON
携带 storageBagItems 键时恒置位，排在全部字段之后保持字段号升序），
Kotlin clearer 判定改为 `count > 0 || present`。

同类系统性残余（**未在本批处理，需登记**）：其余 repeated 列（manualIds /
talentIds / physiqueIds / affixIds / statusData / activePillTypes / usedPill*）
同样缺"列脏且清空"的判别位——属既有协议空洞，非本批引入；建议由 B20b
「typed 投影 + 脏位图」一并收口。

## P3 口径偏差（施工卡需回头改）

- 施工卡写「生成注释删『预览框/选中/拆除高亮』（Vulkan 消费者已随臂 β 退役）」——
  实测**不成立**：C++ `scene_draw.h:759 buildOverlayLayers` 仍消费全部四类
  （网格线/预览框/选中/拆除高亮），退役的是 **Kotlin 逐 rect 绘制臂**。
  删掉这些词会让注释失真 ⇒ 按事实改写（并登记偏差）。
- 施工卡写 `scene_draw.h` 引用守卫名 `SceneOverlayFlagsMirrorGuardTest` ——
  实测该守卫实名 **`SceneOverlayProtocolGuardTest`**（C++ 侧注释引用了不存在的守卫名，
  P3 一并修正）。

---

## B18 收官（P1–P5 全部入库，2026-09-21 13:3x）

**八笔 commit**：`85603146a`(P1-A1) / `ed235c479`(P1-A2) / `ff127abc8`(A2 补遗) /
`ce4e3f84b`(P2) / `802564565`(P3) / `68d9f9ea1`(P4) / `2448af6f1`(P5) / `f55f8337b`(文档+报告)。
完成报告 = `docs/parallel-batches-w5/report-B18-P1-P5-completion-2026-09-21.md`。
用户指令「完成 b18 就结束」⇒ **B20/B19 未启动**（队列仍 queued）。

### 本批新增的两条高价值教训（已写入 skill）

1. **纯 C++ 批的组合门必须补 `--rerun-tasks`**：C++ 改动不进 Kotlin 编译图 ⇒
   `:core:engine:testReleaseUnitTest` 落 **UP-TO-DATE** ⇒ **Diff\* 未对新桥重跑**
   （P2 实测：组合门 5m22s / 18 executed，看似"全绿"实则证据空转）。
   补法 = 追加 `:core:engine:testReleaseUnitTest --rerun-tasks`（18m05s / 53 任务全 executed）。
2. **scalar → repeated 换轨必须补"列携带判别位"**：标量 bytes 的 presence 天然区分
   「未携带」与「携带空值」；repeated 的**零条目与缺省在 wire 层不可区分** ⇒ 列级合并
   （按补丁 presence 清空基线）会让"清空"**静默丢失**（用户可见数据错误）。
   实证：`storageBagItems` 换轨后袋被扣空会保留旧袋 ⇒ 追加 `storageBagItemsPresent=111`。
   **同类系统性残余**：其余 repeated 列（manualIds/talentIds/physiqueIds/affixIds/
   statusData/activePillTypes/usedPill*）仍缺该位 ⇒ 建议 B20b 一并收口。

### 施工卡口径偏差复核（第三类教训：**先验证前提，再决定是否照改**）

P3 §3.2 称「对照对象列级臂已于臂 3 删除」并要求把 bench 两条硬性质改为"对自身趋势"——
**前提反向**（臂 3 删的是**非列级**导出臂），且第二条「两臂落库逐字段全等」是**等价守卫
本体** ⇒ 照改会削弱守卫力。**处置 = 保留断言 + 只校正 KDoc + 登记偏差**（不照字面执行）。

### 关键实测数字（回写 skill/文档）

- **ctest 真实基线 = 1556**（历史记的 1553 为**单进程直跑**口径误记）；本批 +2 ⇒ **1558**；
- 六模块组合门 TOTAL **7882 例 / 0 失败 / 17 既有跳过**；`:core:engine` **3380**；
- `Diff*` 50 类 273 例 0 skip；`SoftwareCanvasBackend*` 106 例 0 skip；
- **JNI 面 87/87 不变**；NDK arm64 `:app:externalNativeBuildRelease` BUILD SUCCESSFUL 3m35s。

---

## B19 批（Room 死列清理）实施 + 收尾（2026-09-21 15:0x–19:2x）

用户 15:06 指令「直接实施 b19，完成后收尾」（中途 15:4x「暂停」→ 18:53「继续」）。
**B19 = Room 死列清理批**（`game_data.battleTeam` 单数 / `aiBattleTeams`），范围 **v51→v52**。

### 三笔 commit

| commit | 内容 |
|---|---|
| `aa740d25c` | 实现：删两实体字段 + `SectOrganizationState.aiBattleTeams` + 两 TypeConverter；`DATABASE_VERSION` 51→52 + `MIGRATION_51_52`（create-copy-drop-rename）+ `52.json`（141→139 列） |
| `6852868b3` | 守卫：`RoomMigrationV51To52Test` 5 例（真实 Room 校验 / 逐字段等价 / v39 全链 / 幂等 / schema 防回流） |
| `76d39b053` + `b368ed61b` | 文档：施工卡 + 报告 + 方案 §7.2 + CHANGELOG + 台账 + 存档勘察报告入库 |

施工卡 `batch-B19-room-dead-columns.md`；完成报告 `report-B19-completion-2026-09-21.md`。

### 关键判据

- **死判复核**：两列全仓零生产者/零消费者；`aiBattleTeams` 唯一读取链终点
  （`organization → SectOrganizationState`）是**生产零消费者 DTO**（`.organization` 生产命中 0）；
  C++ 全仓零出现（只有复数 `battleTeams`）⇒ 零 C++ 改动；两列 `@Transient` ⇒ 不进 `.sav`。
- **旧档窗口勘察判归**：v40 引入复数三列时**只 ADD COLUMN 未搬运** ⇒ **直接删列不搬运**
  （搬运会凭空多出一支队伍 = 行为变更）。
- **删列实现收敛**：新增 `rebuildTableDroppingColumns`（PRAGMA 多列通用 + 幂等 + 索引重建），
  `MIGRATION_49_50` 私有单列实现改为委托（纯抽取）。

### 三条新增高价值教训（已写入 skill）

1. **删列迁移的等价断言不得用 `SELECT *`**：Robolectric legacy cursor 对同一 SQL 串缓存列元数据，
   重建表后拿到**过期列清单** ⇒ 实测 `IndexOutOfBoundsException: Index 139 out of bounds for
   length 139` 与"被删列集算出空集"两种假象；改为按 `PRAGMA table_info` 显式列清单 SELECT。
2. **`ALL_MIGRATIONS` 与 `DATABASE_VERSION` 必须同笔改**：只递增版本漏登记迁移 ⇒ 12 个既有
   "真实 Room 校验"用例全红；`MigrationChainGuardTest` 只断言链自身连续，**拦不住**这类错配。
3. **本仓库 detekt 扫描 test source set**：新测试方法同样受 60 行 `LongMethod` 约束
   （首轮组合门被打回，重构为「夹具 + 断言助手」后绿）。

### 环境事实（重要）

- **同工作区有并行会话**：组合门首跑 1m34s 被外部 `gradlew --stop` 中止
  （`Gradle build daemon has been stopped`）；且**并发跑 ctest 会让 `:core:engine:compileReleaseKotlin`
  出现假失败**（`Discounted multiple Kotlin daemon sessions` 式假红，单跑即绿）⇒ **组合门必须串行跑**。
- **工具坑**：同一条消息里对**同一文件**发两个 Edit 会有一个被静默丢弃（实测两次：
  `ALL_MIGRATIONS` 追加丢失 → 12 例红；测试文件重构丢失）⇒ **同文件编辑必须逐条串行**。

### 实测数字

- `:core:data` **721/0/0/15**（= 716 + 本批 5）；
- 组合门 `BUILD SUCCESSFUL 3m58s`（339 任务 / 23 executed / 0 FAILED，detekt+lint+compile 全绿）
  + `--rerun-tasks` 补强 **12m58s / 229 任务全 executed** ⇒ 六模块 **7888/0/0/17**；
- `Diff*` 273 例 0 skip；Canvas 兜底 106 例 0 skip；桌面 ctest **1558/1558**（`ninja: no work to do.`）。

### 残余（未处理，已在报告登记）

`AIBattleTeam` 模型本体保留（超范围）；`save-system-audit` §16 优先级 1–10 全部未处理；
真机升级路径未实测。**B20（镜像残余专项批）仍 queued**。

---

## 存档审计 §16 十项修复（用户指令「全部解决」，2026-09-21 19:2x–19:5x）

台账 = `docs/parallel-batches-w5/save-audit-16-remediation-2026-09-21.md`（逐项状态 + 拍板清单 + 实施规格）。

### 已解决 4 项（+1 子项），三笔 commit

| commit | 内容 |
|---|---|
| `b35f1d9fb` | **#2** 备份降级如实上报（`SaveOperationStats.postSaveWarning` → `SaveResult.Success(data, warning)` → UI「游戏保存成功（备份未写入）」）；**#4** 真 `.bak` 轮转（`.sav` 覆盖前轮转前一版本，`.bak ≡ .sav` 缺陷根治）；**#8** `restoreFromBackupIfCorrupted` 真实化（原为空函数，现真委派 `restoreFromBackup`） |
| `2ecb51e2b` | **#7** 删档完整性：`clearAllSlotTables` 唯一实现（32 DAO），`delete()` 与 `clearSlotDataQuietly` 共用（原后者只删 2 表 ⇒ 残留 27 表行）+ 补删 4 张漏表；守卫 = 静态扫描锁「@Database 槽位域 DAO 集合 − 豁免 == 清理清单」 |
| `e9c8988ed` | **#1 子项**：`ArchiveConfig.slotIds` 补 slot 6（原 `(1..5)` ⇒ slot 6 永不归档）；+ §16 台账文档 |

### 三条前提勘误（写进台账，防下一轮被误导）

1. **#3「heavy 先删后写 ⇒ 可留半存档」不成立**：实测该链在 `StorageEngine.kt:646` 外层
   `withTransaction` **之内**；真缺口 = `StorageEngineHeavyDataOps.kt:165-173` 单 key 跳过 +
   回填只在 `allRows.isEmpty()` 触发。
2. **#9 逐条判归**：8 个覆盖点里只有 2/4/5/13/15 改存档值（需产品拍板），
   6/12 是索引/镜像（不该动），7/8 是正向兼容补丁（不该动）。
3. **#10 推翻 3 处审计前提**：`SlotLockManager` / `FunctionalWAL` / `DataArchiver` **都是活的**
   （审计称"仅测试/零调用者"错）⇒ 不可照删；真死面已列清单。

### 未实施 6 项及原因

- **待产品拍板 3 项**：#6 保存触发模型（3 选项，推荐"旗标默认关的 onStop 触发"）、
  #9 五子项（读档即收割/生产槽截断/拆除退款/世界重生/巡视槽裁撤）、#1 归档保留策略
  （是否停止物理删主表 / 归档行是否保留 dataBlob）。
- **规格已就绪可开工 3 项**：#3（回填判据 + 跳过分支显式失败）、#5（proto 号让号 +
  守卫补唯一性断言，**wire 面单独走批**）、#10（分 3 笔：存储死面 / Repository 族 / crypto 加密链）。

### 实测

`:core:data` **733/0/0/15**（721 基线 + 12 新守卫）；`:core:data:detekt` 绿；
`compileReleaseKotlin`（core:data + feature:game）BUILD SUCCESSFUL。
**诚实残余**：engine→facade→UI 全链无单元测试基建（构造 StorageEngine 需 8 个 fake）
⇒ 已解决项以「纯函数映射 + 文件层语义 + 静态源码守卫」为证，全链集成测试登记为后续。

---

## §16 剩余项推进（用户「都按你推荐的做」，2026-09-21 19:4x–20:1x）

### 又落库 3 笔（共 7/10 项已实施）

| commit | 项 | 内容 |
|---|---|---|
| `0f6215981` | **#1**（Explore-2 实施） | 归档载荷可还原：`dataBlob` 存全量序列化行（保留搬出主表 + 保留期不变，仅消除不可逆性）；守卫 `ArchivePayloadRoundTripTest` 3 例 |
| `908f24180` | **#5** | proto 号唯一性：`pendingTraitAdds` 让号 **162→1002**（更老的 `prisonerSpiritRootFilter` 保留 162）；守卫 `ProtoNumberUniquenessTest` 2 例（按类作用域判重 + 方向锁） |
| `853bb2d8b` | **#3** | heavy 数据保全：缺失 key 按域表回填（新增 `StorageEngineHeavyDataBackfill.kt`）+ 被跳过 key 不被下次保存抹掉（`heavyPrefixesToClear` 纯函数）；守卫 `HeavyDataPreservationTest` 5 例 |

实测：`:core:domain` **1743/0/0**、`:core:data` **705/0/0/15**（含新守卫；总数下降 = #10 删掉了 crypto 测试）、`:core:data:detekt` 绿。

### ⚠️ 未提交（工作区仍脏，下一轮必须先处理）

1. **#9**（`GameEngineLifecycleOps.kt` + `MerchantInventoryRepairGuardTest.kt`，core:engine）：
   商人修复性刷新加"仅当从未生成"锚（`shouldRepairMerchantInventory`），**编译/测试未跑绿**
   （`:core:engine` 多次被环境打断：Kotlin 守护进程被杀 / 命令 SIGTERM）。
2. **#10 死代码第一刀**（Explore-4 已改完但**未验证**）：删 `SaveStorageImpl`/`ChangeTracker`/
   `SchemaVersion`/`SerializationQuota`/`crypto/` 存档加密链（保留 `.secure_key` 网络签名链）/
   `ArchiveDaos` 4 查询方法/`StorageFacade` 死方法/`AppModule`+`CoreModule` 绑定等，
   `:app:compileReleaseKotlin` **当前红或未确认**（环境不稳定，错误行未取到）。

### 环境教训（本轮回写 skill）

- **Explore 型 worker 是只读沙箱**：`Edit`/`Write` 不可用，只能靠 Bash 落盘（部分场景 Bash 写文件也被安全策略拦）。
  ⇒ 实施类任务应派 **general-purpose** worker，不要派 Explore。
- 4 个 worker 并发跑 Gradle ⇒ **Kotlin 守护进程被反复杀**（`Connection to the Kotlin daemon has been
  unexpectedly lost` / `daemon has been stopped` / `compileReleaseJavaWithJavac` 假红 / SIGTERM）。
  ⇒ 同工作区并发上限建议 **1 个构建者**；门禁必须串行。




---

## B18 遗留登记补齐（第 9 笔 `13221fbda`）

用户选择「先补 B18 遗留登记再停」⇒ 两件事均已做完：

### ① P1 §1.6 勘察点② 实测登记（新增 `TypedEnvelopeSizeBenchTest`）

- **体积**：typed 比旧 JSON 文本大 **11.3%–20.8%**（9 集合全部）⇒ **无需任一集合暂留 JSON**；
- **Kotlin 消费侧耗时**：typed 重建慢 **1.23–2.12×**（绝对值 0.005–0.027 ms/3 实体）；
- **C++ 编码侧耗时不可测**（旧编码器已删）⇒ **净收益不可判定，不得声称"typed 更快"**；
- 确定收益 = 语义/维护面（单一 typed 承载）。台架顺带做真实样本等价复核。
- 方法可复用：javalite 原样重建旧 wire 形态 + 真 C++ 编码器产新形态 ⇒ 同树精确对拍。

### ② P4 独立门补齐（skill 文档口径，第 6 次尝试才过）

`testReleaseUnitTest`（六模块）+ `:core:engine:detekt` + `:feature:game:detekt` + `:app:lintRelease`
→ **BUILD SUCCESSFUL 20m13s**（319 任务 / 43 executed）；`:core:engine` **3381 例 0 失败 0 skip**。

### ③ 本工作区新增高危环境事实：**存在并行会话（B19 在飞）**

取证：① 15:21 出现未跟踪文件 `RoomMigrationV51To52Test.kt`（Room V51→V52，正对应 B19）；
② 15:2x 起 core:data/core:domain 的**已跟踪**文件被改（CollectionConverters / GameDatabase /
GameDatabaseMigrationsV50 / GameData.kt / SectOrganizationState.kt 等）；③ 有外部 `gradlew --stop`。

**四类环境事件（非代码失败，已在报告 §3.6 逐条留痕）**：
1. `Gradle build daemon has been stopped: stop command received`（外部 --stop 杀守护）；
2. `source file or directory not found: ...BattleSystemu4F24u5BB3Ops3.kt`——**守护损坏致中文
   文件名转义成 uXXXX**；`./gradlew --stop` 后**同命令 21s 编译成功** ⇒ 别改仓库配置；
3. 用户「暂停」终止后台任务（日志停在编译中、无失败段）；
4. 外部未跟踪文件打回 **bare `detekt`**（全模块口径含 `:core:data`）⇒ 改用 skill 文档口径
   （`:core:engine:detekt :feature:game:detekt :app:lintRelease`）绕过，**未触碰其文件**。
5. **自身真遗漏**：新增台架只跑过 `--tests` 过滤测试、**未过 detekt** ⇒ 组合门暴露 5 项
   `ImplicitDefaultLocale`（`String.format` 隐式 locale）⇒ 改 `Locale.ROOT` 后通过。
   **教训：新增 Kotlin 文件后必须先单独跑一次 `:module:detekt`**。

### 收尾时的并行会话态势（17:4x 观测）

B19 已被派发并在飞：新增批次卡 `docs/parallel-batches-w5/batch-B19-room-dead-columns.md`，
且其改动已**越入 core:engine**（`gameview/GameDataTransientFace.kt` 被改，未提交）。
⇒ **本批（B18）9 笔提交与其完全隔离**；但**后续任何门禁的"六模块 TOTAL"都会含其增量**，
验收轮需先确认其工作区状态（或等其提交）再跑 bare `detekt`。



---

## §16 十项修复收官（2026-09-21 20:3x–21:1x）：10/10 全部实施入库

10 笔 commit：b35f1d9fb(#2/#4/#8) / 2ecb51e2b(#7) / e9c8988ed(#1 slot6+台账) /
0f6215981(#1 归档可还原) / 908f24180(#5 proto 号) / 853bb2d8b(#3 heavy 保全) /
bf8394bbc(#9 商人锚) / 5a421f3d6(#10 死代码第一刀) / fd6451de1(副产物还原) /
870be9771(#6 后台保存触发，旗标默认关)；台账收官 = 95752622a。

收尾组合门：BUILD SUCCESSFUL 22m50s / 339 任务 / 78 executed；六模块 7861/0/0/17；
Diff* 50 类 273 例 0 skip。（相对 B19 基线 7888：-27 = #10 删掉的 crypto 死测试；
+29 = 本批新增守卫。）

### 三条环境/协作教训（务必回写 skill）

1. Explore 型 worker 是**只读沙箱**（无 Edit/Write，部分场景 Bash 写文件也被拦）：实测
   Explore-3 完全无法落盘、Explore-4 报 "Tool Edit not found"（后绕道 Bash 才落盘）。
   => 实施类任务必须派 general-purpose。
2. 并发 Gradle 是本机最大杀手：4 worker 并发 => `daemon has been stopped: stop command
   received`（他人 gradlew --stop 杀守护）、`New files were found ... a process is still
   writing to the target directory`、`Connection to the Kotlin daemon has been unexpectedly
   lost`、`compileReleaseJavaWithJavac FAILED` 等**假红**，串行重跑即绿。
   => 并发构建上限 1；任何人不要执行 gradlew --stop。
3. 中文文件名全量重编会炸：:core:engine 全量重编时中文源文件名被转义成 uXXXX，报 21 处
   `source file or directory not found` 并级联数百条"未解析引用"（本机长期依赖增量编译；
   CI 在 Linux 无此问题）。=> 遇此不要改代码。

### 实现要点（供后续批参考）

- #5 proto 号：让号原则 = **新字段让老字段**（pendingTraitAdds 162->1002，老字段留 162）；
  守卫必须**按类作用域判重**（proto 号 per-message 语义，跨类重复合法，文件级扫描会误报）。
- #3 勘误：审计"先删后写可留半存档"不成立（在 StorageEngine.kt:646 外层 withTransaction 内）；
  真缺口 = 单 key 缺失不回填 + 跳过 key 被下次保存抹掉。
- #1 数据安全向：保留"搬出主表"+保留期不变，只把 dataBlob 由 "" 改为全量序列化行。
- #6 灰度纪律：产品决策项一律「旗标默认关 + 纯函数判定直测」，默认关即零行为变更。
- #10 分刀：Room schema 变更（6 张冗余表）不得混入死代码批，必须单独走批带 migration。
