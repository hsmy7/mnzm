# 2026-09-20 工作日志

## B15 / R6.1 图集离线化（消运行时 Canvas 拼装，运行时只 upload）

**用户指令**：读 `docs/parallel-batches-w5/batch-R6A.md`，严格实施 B15，按文件内验收门
自检并出报告。批次身份三重确认：**B15 = R6.1 = `batch-R6A.md`**（文件名 ≠ 批次号）。

### 核心结论

B15 前三条支路（Canvas 软渲 / RGBA 回退臂 / RGBA mip 链）共用
`SectAtlasAssembler.buildAtlasBitmap` —— **启动期在设备上**逐精灵
`BitmapFactory.decodeResource` + `Canvas.drawBitmap` 拼 2048² ARGB_8888 位图。
本批把「像素来源」整体搬到**构建期**，运行时只做一次性解码/映射 + upload。

### 关键技术决策（可复用）

1. **产物形态 = 裸像素 RAW + JSON 清单**（非 PNG）：只有 RAW 能 `FileChannel.map`
   **零拷贝**（不占 Java heap、不 decode）且软渲臂可一次 `copyPixelsFromBuffer`
   （直通 alpha RGBA8888 与小端 ARGB_8888 **逐字节等值 ⇒ 无需 swizzle**）。
   代价 = 37.33 MiB 入库资产，换启动期 heap 峰值 53.26 MiB → 0 / 33.26 → 16 MiB。
2. **同源架构**：产物由 `build-atlas.mjs` 的 `main()` **内联**产出，复用 ASTC 图集
   同一份 `contents`；把「源 → 预乘 → lanczos3 槽位」抽为共享
   `lib/atlas-offline-rgba-lib.mjs::loadPremultipliedRgba` ⇒
   「两条图集路径精灵内容同源」成为**结构事实**而非比对断言。这是本批最重要的架构手法。
3. **`SectAtlasAssembler` 退役形态 = 「类本体转测试夹具」（非删除）**：因为
   `downscaleWithBilinearChain` 是 `SoftwareCanvasBackend.drawPreScaled`（红线路径）的
   **单一实现**，`buildingAtlasDrawableMap`/`tileDrawableRes` 是既有守卫测试入口。
   338 → 111 行，只删拼装路径。

### 像素重采样核实测（重要经验，跨批复用）

**结论：无任何重采样核能与 Skia 双线性逐位对齐。** 四组候选实测（源 → 槽位半尺寸）：

| 配方 | 超阈值（>8/255）占比 |
|---|---|
| `lanczos3`（产物所发） | 4.68% – 12.50% |
| `linear`（双线性同核） | 4.68% – 12.50% |
| 链式减半（双线性分步） | 10.47% – 26.50% |
| 精确区域平均（box） | 3.59% – 24.00% |

⇒ **等价性守卫必须四层口径**：硬门 = 结构等价 + golden 校验和 +
「按产物真实生产链的确定性逐位复现」；软门 = 消费面容差对照（阈值基于实测分布，
并显式登记为「守卫退化」而非「证明等价」）。

**坑 1**：链式缩放与单步缩放**不等价**。产物是两段（`源→槽位→半槽位`，因复用
ASTC 图集 contents），运行时拼装是单步。任何「源 → 半槽位」单步重建都与产物逐位不符
（GRASS1 maxDiff=255）⇒ 不能假定缩放可合并。
**坑 2**：等价性守卫的「运行时配方」**必须走预乘空间**再缩放（再解预乘），否则透明区
未定义颜色参与插值 ⇒ maxDiff=255 假阳性（GRASS1 16.5%）。

### 其它踩坑

- **Gradle 隐式依赖**：任务读了 `:core:engine` 的 output（`SpriteAtlasDef.kt`）必须
  显式 `dependsOn ':core:engine:generateSpriteAtlasDef'`，否则报
  `implicit dependency` 配置错误。
- **静态门禁必须先去注释**：退役后 KDoc「历史落点」段会刻意提及旧函数名 ⇒
  直接扫全文**假红**。剥离 `/* */` 与 `//`（保留字符串字面量与 `"""`）后扫描。
- **Gradle 单元测试工作目录 = `android/feature`（不是 `android/feature/game`）**：
  仓库既有 `EdgeKtxSyncTest` 用 `../../app/...` 即是佐证。定位仓库文件按此基准，
  并兜底两级。
- **`FileChannel.map` 返回的 ByteBuffer 默认 `BIG_ENDIAN`**（JDK 规范，与
  `allocateDirect().order(nativeOrder())` 不同）。裸像素按字节消费，测试不应断言
  `nativeOrder()`。
- **产物 manifest 的 sprites 只有 `name/sha256/bytes`**（无 x/y/w/h）——落位几何
  应由 `SpriteAtlasDef` 权威**推导**（`round(x*0.5)` / `slot>>1`），不要往 manifest
  塞冗余几何。
- **Gradle 并行门互斥**：组合门跑着时另起 Gradle 命令会撞
  `journal-1.lock（拒绝访问）`；须等门结束或 `gradlew --stop`。
- **`mergeReleaseResources` Windows 文件锁**：实测在组合门第二次重跑时触发
  （`Unable to delete directory ... merged_res\release\mergeReleaseResources`）。
  处置口径 = `gradlew.bat --stop` + `rm -rf app/build/intermediates/merged_res/release/mergeReleaseResources`
  + 清 `~/.gradle/caches/journal-1/journal-1.lock` 后重跑，**勿改代码**。
- **detekt 打回两轮，都是「重构引入的残留」**：第一轮 `stripKotlinComments` 复杂度
  （CyclomaticComplexity 28 > 15 / NestedBlockDepth / ComplexCondition），拆小函数后过；
  第二轮 `NativeSurfaceViewTest.offlineAtlasSpecOrNull(ctx)` 的 `ctx` 参数在路径 helper
  重构后**不再被使用** ⇒ `UnusedParameter`。教训：**重构成 helper 后要顺手检查参数表**，
  否则 detekt 会在全量门末尾才打回（约 48 min 后）。
- **全量组合门实测耗时 ≈ 48 min**（233 tasks 全 executed），比批次文件预估的 20–35 min
  长；各阶段实测：编译 ~30 min → 测试 ~12 min → detekt/lint 尾段 ~5 min。规划时按 50 min 预留。
- **检测 XML 汇总的「假新鲜」陷阱**：`build/test-results/` 下同时存在
  `testDebugUnitTest`（历史）与 `testReleaseUnitTest`（本轮）两种目录；且**过滤 run**
  （`--tests`）只重写被选中类的 XML，其余保留旧文件。⇒ 汇总脚本必须（a）只收
  `testReleaseUnitTest*` 目录，（b）打印 XML mtime 范围并做陈旧告警。
  已固化为 `android/scripts/summarize-junit-xml.py`（支持 `--json`）。
- **测试类名前缀过滤要看 `classname` 而非 `name`**：Gradle XML 的 `testsuite/@name`
  与 `testcase/@classname` 都是 FQCN；按「简单类名前缀」过滤须先 `rsplit('.', 1)`。

### 门禁

- 受影响测试套全绿：`AtlasOfflineRgbaSyncTest` 7 / `AtlasAsyncPipelineMipChainTest` 4 /
  `NativeSurfaceViewTest` 27 / `SectAtlasAssemblerDownscaleTest` 4 /
  `SectAtlasBuildingDrawableGuardTest` 3 / `SectAtlasTileDrawableGuardTest` 2 /
  `EdgeKtxSyncTest` 3 / `ReproGroundScaleTest` 1。
- 等价性守卫四层全绿（3a 41/41 逐位复现；3b alpha 3.3575%、颜色 16.4163%）。
- `generateOfflineRgbaAtlas` 实跑 + 二次 `UP-TO-DATE` 实证。
- 桌面 GTest / 组合门见完成报告（`docs/parallel-batches-w5/report-B15-completion.md`）。

## W5 收口批接手：交接文档验证执行（handover-closing-batch）

**任务**：读 `docs/parallel-batches-w5/handover-closing-batch-2026-09-20.md`，按 §3 SOP 跑完 §2 全部待验证项。

### 发现并修复的真实缺陷（交接文档未预期）

**C 组 `GameDataTransientFace` 注解反射静默失效**：原实现用
`GameData::class.java.declaredFields.filter { it.isAnnotationPresent(Transient) }`
枚举 @Transient 面，实测**枚举为 0 个字段** ⇒ `carryOver` 变 no-op，legacy 臂
（旧全量 JSON 往返）把 `slotId` 打回 0，`GameDataFieldPatchGuardTest` 3 个用例红。

根因链：GameData 字段是**主构造器 var 参数**；`kotlinx.serialization.Transient` 的
`@Target` 含 `PROPERTY`/`VALUE_PARAMETER` 但**不含 `FIELD`** ⇒ Kotlin 编译器不把注解
写到 JVM 字段上。实测 declaredFields=150、带任何注解者 **0**；构造器参数注解亦空，
且 `isParamNamePresent=false`（无 `-parameters`，参数名不可得）⇒ 注解反射与构造器
参数反射**两条路都不通**。

**根治（权威面法）**：改以 **kotlinx 序列化 descriptor 为权威**——
`GameData.serializer().descriptor.elementNames`（137）与「非 static、非合成实例字段」
（146）求**差集**，精确得 **9 个** @Transient 字段（slotId / autoSaveIntervalMonths /
aiBeastEncounterTargets / aiSectDisciples / aiSectBeastSkipCooldowns / lockedBeastIds /
aiSectBeastDirectTargets / battleTeam / aiBattleTeams），双向无残余。新增 @Transient
字段自动纳管，**且不引入 kotlin-reflect 生产依赖**（该库仅 testImplementation）。

### 踩坑（可复用）

- **单进程直跑 GTest 必须先 cd 到 `android/app/src/main/cpp/gamecore`**：
  `data_store_test.cpp::locateGameDataJson()` 候选路径**相对 cwd**，ctest 用
  `WORKING_DIRECTORY` 钉为 gamecore 源码根。在 `build/desktop-test` 下直跑会 6 个
  DataStoreGuardTest fail（"game-data.json 不存在"）——**非代码缺陷**，是夹具 fail-fast。
  判定实验正确姿势：`cd .../gamecore && ./build/desktop-test/test/game-core-tests.exe`。
- **JAVA_HOME**：本机 Android Studio 的 jbr 目录**不完整**（无 lib/jvm.cfg）；
  正确路径 `C:/Users/cp050/.jdks/jdk-21.0.12.1+1`。
- **`-Dgamecore.jni.path` 必须用 Windows 原生路径**（`C:\...\libgamecorejni.so`）：
  Git Bash 的 `/c/...` 被 Gradle 解析成 `C:\c\...` ⇒ UnsatisfiedLinkError。
- **桥构建脚本无法从 Bash 调用**（安全策略拦截 pwsh），PowerShell 工具又吞输出
  ⇒ 改为**直调 clang++ 手工复现脚本命令**（等价、可控）。命令见下。
- **pip 无外网**：`pip install pyyaml` 不可用 ⇒ ci.yml 合法性只能人工核对缩进/step
  结构（已核：`jni-count-gate` 与 `build` 同级缩进 2 空格、steps 完整）。
- **build/ 目录残留陷阱**：`app/build/generated/sprite/footprint_table.h` 在任务删除后
  仍以旧时间戳留在 build/ 中（Gradle 不清旧产物）。判断"链已断"须**删残留 + 跑
  preBuild + 确认未重生**，不能只看文件是否存在。

### 桌面 JNI 桥手工编译命令（等价 build-desktop-jni.ps1）

```bash
TC=/c/Users/cp050/llvm-mingw/llvm-mingw-20260616-ucrt-x86_64/bin
SRC=android/app/src/main/cpp/gamecore
$TC/clang++.exe -shared -fPIC -std=c++20 -O2 -ffp-contract=off \
  -static -static-libgcc -static-libstdc++ \
  -I $SRC/include -I $SRC/third_party -I $SRC/jni-include \
  $SRC/jni/GameCoreJni.cpp \
  $SRC/src/rng.cpp $SRC/src/game_core.cpp $SRC/src/json_codec.cpp \
  $SRC/src/execute_dispatch.cpp $SRC/src/dispatch_w4a.cpp $SRC/src/dispatch_w4b.cpp \
  $SRC/src/dispatch_w4c.cpp $SRC/src/dispatch_w4d.cpp $SRC/src/dirty_tracker.cpp \
  $SRC/src/gameview_encode.cpp $SRC/src/disciple_store.cpp \
  -o android/core/engine/build/desktop-jni/libgamecorejni.so
```

### 验证进展（本轮实测）

| 项 | 结果 |
|---|---|
| A：ctest 全量 | ✅ 1556/1556 |
| A：单进程直跑（UAF 判定） | ✅ exit=0、1553/1553、23.8s |
| A：NDK arm64 构建 | ✅ `:app:externalNativeBuildRelease` SUCCESSFUL |
| B：ExplorationPatrolRouteTest | ✅ 全类绿 |
| C：GameDataFieldPatchGuardTest | ✅ 修复后转绿（原 3 红） |
| C：DiffMirrorArmConvergenceTest | ✅ 三臂收敛全绿 |
| D：check-jni-count.mjs | ✅ 91/91 双桥无扩散 |
| D：ci.yml 合法性 | ✅ 人工核对通过 |
| E：:app 测试 + preBuild 链 | ✅ 2 类绿；preBuild 跑通、footprint_table.h 不再生成 |
| F：Equivalence/Bench 测试 | ✅ 绿（proto 再生成亦通过） |
| ③ 全量组合门（第 1 跑） | ❌ 环境故障：30m01s 时 `:app:mergeReleaseResources` 文件锁（99 executed、**测试面零证据**）⇒ 清锁后整门重跑 |
| ③ 全量组合门（第 2 跑） | ✅ 跑完（**55m07s、230 executed**）——**测试面全绿**：7876/0 fail/17 既有 skip；`Diff*` 50 类 273 用例 **0 skip**；`SoftwareCanvasBackend*` 10 类 106 用例 0 fail。但 **detekt 打回 1 个真实问题**：`PatrolBattleSystem.kt:542 UnusedParameter`（B05 重构残留） |
| ③ detekt 修复 | ✅ 删除 `applyVictoryRewards` 的 `state` 参数（B05 把灵石入账移入 `applyVictoryGdChanges` 后成空参数）+ 调用点同步；单跑 `:core:engine:detekt` 绿 |
| ③ engine 收敛验证 | ⏳ 后台（`--rerun-tasks` 全量 engine 测试 + detekt + compile） |

### 组合门第 2 跑结果（2026-09-20 09:32）

`230 actionable tasks: 230 executed`，测试面**全绿**：
- core:engine **3374**/0 fail（= 预期 3372→3374 ✓）
- core:domain 1743 / core:data 716（15 既有 skip）/ core:ui 146 / feature:game 887 / app 1010
- **TOTAL 7876 tests / 0 fail / 17 既有 skip**
- **`Diff*` 50 类 273 用例 0 失败 0 skip**（对拍桥真加载 ✓）
- `SoftwareCanvasBackend*` 10 类 106 用例 0 失败（Canvas 红线 ✓）

**但 detekt 打回（唯一失败点，真实代码问题非环境）**：
`core/engine/.../exploration/PatrolBattleSystem.kt:542:9 Function parameter 'state' is unused`
——B05 根治把灵石入账移入 `applyVictoryGdChanges` 链后，`applyVictoryRewards` 的 `state`
参数成空（skill 记录的"重构后遗留未使用参数"经典坑）。**已修**：删参数 + 调用点同步
（非加 `@Suppress`——该 `state` 是 B05 重构的纯历史残留，不像
`applySurvivorSoulAndAttribute.target` 那样是"API 决策域"语义形参）。

### 组合门第 1 跑环境故障处置（2026-09-20）

`./gradlew testReleaseUnitTest --max-workers=1 --rerun-tasks ... detekt compileReleaseKotlin
lintRelease` 跑到 **30m01s** 时 `:app:mergeReleaseResources FAILED`：
`java.io.IOException: Unable to delete directory ...merged_res/release/mergeReleaseResources`
（`99 actionable tasks: 99 executed`——**测试根本未跑**）。这是仓库既有已知环境故障
（Windows 文件锁），非代码红。处置：`./gradlew --stop`（输出 "No Gradle daemons are
running"，但句柄仍在）→ `rm -rf app/build/intermediates/merged_res/release/mergeReleaseResources`
→ `rm -f ~/.gradle/caches/journal-1/journal-1.lock` → **整门重跑**。
**教训**：此错早于 `testReleaseUnitTest`，故第 1 跑的 "99 executed" **不能**当门结果。

### 副产物说明（勿混装提交）

`android/app/src/main/assets/atlas/atlas-rgba-manifest.json` 的 `generatedAt` 被 B15
`generateOfflineRgbaAtlas` 任务刷新（**像素逐位未变**，仅时间戳）⇒ 建议**不纳入**本次提交。
**处置**：`git checkout --` 还原，确认该文件已回到 HEAD 版本（`git status` 干净）。

### 分组提交（2026-09-20 完成）

按交接文档 §0 六组独立 commit + 文档组，共 **7 个提交**（分支 `main`）：

| 序 | commit | 组 | 内容 |
|---|---|---|---|
| 1 | `b26c9d4f0` | A | C++ 数据 DB RCU 索引快照（UAF 根治）：`index_snapshot.h` 新 + `trait_db.h`/`recipe_db.h`，3 files +137/−40 |
| 2 | `48d45baf2` | B | B05 巡逻灵石入账 + detekt 残留参数清理：`PatrolBattleSystem.kt` + 测试，2 files +30/−14 |
| 3 | `39a161e4d` | C | @Transient 反射枚举失效根治（**5 文件同 commit**）：`GameDataTransientFace.kt` 新 + 两臂 + 守卫 + 注释，+128/−50 |
| 4 | `7e4ecf31f` | D | JNI 面计数门禁：`check-jni-count.mjs` + `jni-count.baseline.json` 新 + `ci.yml`，3 files +155 |
| 5 | `96c253e37` | E | 退役 `generateFootprintHeader` 死管道：`app/build.gradle` + `FootprintTableSyncTest.kt`，2 files +9/−136 |
| 6 | `35ee16261` | F | 镜像 proto 馈送面 + 分段投影基准：proto + codec + 权威接线 + 3 测试，6 files +74/−9 |
| 7 | `2cb00fa54` | 文档 | 方案 §7.3 + CHANGELOG + cpp-engine + 交接/验收/B18 三文档 + 台账 + 本日志，8 files +569/−2 |

提交手法（skill 已固化）：**`Write` 写 `.git/MSG-*.txt` 再 `git commit -F`**，避开
heredoc + `-F -` 被 SIGTERM 打断的坑；提交后 `rm -f` 清理。commit 输出末尾的
`reg.exe blocked by security policy` 是 Git 钩子噪音，**非提交失败**（以 `git log` 复核为准）。

### B18 判据缺口核查（2026-09-20，`a17a3330f`）

用户追问「B18 为什么不做了」，核查后确认**到期判据（硬前置）不满足**：

| 判据 | 状态 | 实测依据 |
|---|---|---|
| ① 发版周期经过 | ❌ 不满足 | `version.properties` 仅 1 次提交（`c13d65157`），仍 `4.01.14`；**版本号从未变更** |
| ② 零回滚事件可查证 | ⚠️ 证据面缺失 | 仓库**无 `RenderMetrics` 回滚计数器**；全仓 `rollback` 命中均为业务语义（`FunctionalWAL` / `TransactionRngRollbackTest` / `GameStateStoreRollbackTest`）|

**五条臂 git 实测建立时点**（距核查约 44h，未历发版）：R2.2 `6b3354708` 09-18 17:45 /
R2.3-字段级 `4d053b9be` 09-18 21:10 / R2.3-弟子行 `b7b68b4ba` 09-18 21:25 /
R2.3-投影态 `6fd9fe03d` 09-18 21:42 / R3.2-换轨 `242440778` 09-19 08:30 /
R3.8 `3a7d0e3de` 09-19 19:06。

**删除影响面**（旗标生产引用点，不含测试与定义本身）：
`mirrorProtobufTransport` 5 引用 / 1 测试文件；`gameViewProjection` 11 / 4；
`dirtyColumnExport` 5 / 2。

**判定**：判据①是时间门（等待即满足），判据②是**证据面本身缺失**（须先补埋点或
改用崩溃快照/Bugly，非等待可解）⇒ 已把缺口 + 影响面 + 开工前置三步登记进
`batch-B18.md`。用户曾要求「强行开工删回滚臂」，因违红线 §3（未附证据即开工 = 验收
打回）且属删除操作，未执行；改为登记文档。**开工前置**：补埋点 → 发版起算 → 零回滚证据。


---

## B18 臂 2（投影臂退役）实施完成 — `efba3ee72`

**起点**：`docs/parallel-batches-w5/handover-b18-wip-2026-09-20.md`（臂 2 半程中断现场 + §2 续作施工卡）。
接手时工作区**不可编译**（`gameViewProjection` 定义已删，3 处生产引用 + 4 个测试文件引用未清）。

**按施工卡 §2.1–2.8 完成**：
- §2.1 `StateSyncService` 三处 KDoc 残留（:58/:413/:465）清理
- §2.2 `GameEngine` 三块 UI 消费（resourcesHeader/configEcho/eventLog）删 if/else 回滚分支，
  恒委托 `gameViewStore.*`（**编译断点消除**）
- §2.3 `MirrorConsumerSurfaceGuardTest` 旗标断言 → 反向断言（`gameViewProjection` 不得回流）+
  删未用 `NativeEngineFlag` import
- §2.4 `GameDataFieldPatchGuardTest` 两臂对照 → **测试侧 golden 夹具**（新增 `goldenRoundTrip`
  冻结被删生产臂最终语义）；`feed` 单臂化、`Feed` 增 `initialGameData` 基准、防复发守卫去
  projection 循环、增 `jsonObject` import、删未用 flag import
- §2.5 `GameViewStoreGuardTest` 删"灰度旗标关闭即不馈送投影"，补"镜像馈送恒推进投影"
- §2.6 `MirrorSegmentProjectionBenchTest` 单臂化（去对照臂计时与三条两臂断言，保留
  "投影块 == `resourcesViewOf(store)`" 全等 + 趋势数字打印）
- §2.7 `GameEngineCoreMonthOps:167` KDoc 去"回滚臂专用"措辞
- §2.8 验证 + 提交 + `scripts/jni-count.baseline.json` 91 → 90（含 `_doc` 口径同步）

**顺手清理**：`StateSyncService.applyDirtyProto` 内 KDoc 中"R2.3 第二波灰度/关旗标即回"措辞。

**§7.1 前置核查结论**：`DiffMirrorArmConvergenceTest` **直接调 `applyDirty`/`applyDirtyProto`**，
不走旗标驱动（F2 的 JSON 路径作为**测试侧对拍臂**保留）⇒ **无需改动**。`applyDirty` 生产入口
仍存在（`applyDirty`/`applyDirtyProto`/`applySnapshot` 三入口），与 `batch-B18.md` 登记的
"F2 通过 `applyDirty` 直调不依赖旗标"一致。

**验证数字（亲跑）**：
- 组合门四任务全绿：`testReleaseUnitTest --max-workers=1 --rerun-tasks`（**BUILD SUCCESSFUL 4m15s**）
  + `detekt` + `compileReleaseKotlin` + `lintRelease`（**BUILD SUCCESSFUL 5m1s**）
- Kotlin 全量单测 **3374 passed / 0 failures / 0 errors / 256 skipped**（324 个测试类）
- 臂 2 目标套 31 passed / 0 fail：gameview 5 类（GameDataFieldPatch 6 / GameViewStore 6 /
  Bench 2 / EventEnvelope 4 / DiscipleProjection 4·1skip）+ SurfaceGuard 6 + ProtoFeedEquivalence 2
  + DiffMirrorArmConvergence 1·1skip（`assumeTrue` 桌面桥不可用，既有常态）
- 桌面 GTest 双验证：`ctest` **1556/1556**（358.73s）+ 单进程直跑 **1553/1553 exit=0**
  （`cd` 到 gamecore 源码根，避 `DataStoreGuardTest` 夹具 fail-fast）
- JNI 门禁：`total=90/90` 双桥无扩散（基线已下调）
- `ninja: no work to do.` — 臂 2 零 C++ 改动，符合预期

**纪律落地**：「删臂后守卫不得失去对照面」→ `GameDataFieldPatchGuardTest.goldenRoundTrip`
把被删生产臂的语义冻结为测试侧实现（原样搬运 encode→覆盖→decode + `GameDataTransientFace.carryOver`），
`feed`（生产路径）继续与之逐值对照 —— 这是本批最具复用价值的模式。

**提交链**：`df6b70d5a`（臂1）→ `efba3ee72`（臂2）→ `510f9fd28`（台账转进行中）。

---

## B18 臂 3（列级臂退役）实施完成 — `a7de1360a`

**C++**：`game_core.h` 删 `setDirtyExportColumn` / `dirtyExportColumn()` /
`columnLevelDirtyExport_` 成员，`exportDirtyProto` 恒列级；`GameCoreBridge.cpp` 删
`nativeSetDirtyExportColumn` JNI。**异构写入锁存 `columnExportBlocked_` 保留**——
月/年/旬边界外写入路径的防漏报正确性机制（回退全量一封），不是回滚臂。

**Kotlin**：`NativeEngineFlag.dirtyColumnExport` 删；`GameCoreBridge` decl 删；
`GameEngineCoreAuthoritativeOps` 推送点删；`GameViewMirrorCodec.decodeView` 缺省值
翻转为生产形态（`includeDiscipleJson=false` + `discipleRowsAsPatches=true`）；
`StateSyncService.applyDirtyProto` 改单参重载 + `internal applyDirtyProtoWith(decode)`
golden 注入点。

**守卫**：`DiffColumnExportMergeConvergenceTest` 全行应用臂转 golden
（`goldenApplyFullRows` 显式 `discipleRowsAsPatches=false`）；
`MirrorConsumerSurfaceGuardTest` 补 `dirtyColumnExport` 反向断言 + 更名"B18 三条臂已退役"；
`GameViewDiscipleProjectionTest.jsonViaTree` 显式传参；`MirrorSegmentProjectionBenchTest`
`measureColumn` 去旗标 try/finally。

### 关键踩坑（新增，已写进 skill）

1. **`decodeDiscipleDelta` 的 `when` 分支顺序 = 补丁 > JSON 树 > typed 全行**。
   我先把 `discipleRowsAsPatches` 缺省值翻成 `true`，结果 `includeDiscipleJson = true`
   **完全不可达** —— 3 个用例 `NoSuchElementException`（`changed["disciples"]` 恒缺）。
   只翻 `includeDiscipleJson` 缺省值**不足以表达形态**。修法：`decodeView` 两个形态参数
   保持显式传参纪律，依赖旧形态的对照面必须传 `discipleRowsAsPatches = false`。
   **教训**：删臂时若一个函数有"多重互斥形态参数"，翻缺省值前先读分支顺序（`when` 优先级），
   否则对照臂会**静默失效**（断言不再覆盖原语义，却仍绿/或红得莫名其妙）。
2. **`StateSyncService` 不得 `import com.xianxia.sect.proto.gameview.*`** ——
   `MirrorConsumerSurfaceGuardTest` 的"信封解码只经 GameViewMirrorCodec"门禁把
   `proto.gameview` 的 import 面**锁死在 codec + GameViewDiscipleRows 两处**。
   需要类型时用**全限定名**声明（`applyDirtyProtoWith` 的解码器形参即如此），
   引 import 会直接让门禁红。
3. **detekt 未用 import**：删调用点后同一文件里的旗标 import 变孤儿，detekt
   `UnusedImports` 会红（`GameEngineCoreAuthoritativeOps.kt` 的 `NativeEngineFlag`）。

**验证数字**：组合门 `BUILD SUCCESSFUL 11m12s / 94 任务全 executed`；Kotlin 全量
**3374 passed / 0 failures / 0 errors**（324 类）；ctest **1556/1556**（390.64s）+
单进程 **1553/1553 exit=0**（有 C++ 改动，ninja 重建 56/56 链接通过）；
JNI 门禁 **89/89**（基线 90→89）。

**提交链**：`df6b70d5a`（臂1）→ `efba3ee72`（臂2）→ `a7de1360a`（臂3）。
**剩余**：臂 β（`sceneStoreRender`，含 C++ drawAllTiles 大块 + `:feature:game` 全测 +
NDK arm64）、臂 γ 与吸收项。
