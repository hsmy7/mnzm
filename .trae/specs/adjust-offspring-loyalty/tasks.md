# Tasks
- [x] Task 1: 修改普通子嗣初始忠诚度
  - [x] 将 `createChild` 函数中的 `loyalty = Random.nextInt(30, 61)` 改为 `loyalty = Random.nextInt(70, 101)`
- [x] Task 2: 修改跨宗门子嗣初始忠诚度
  - [x] 将 `createCrossSectChild` 函数中的 `loyalty = Random.nextInt(60, 90)` 改为 `loyalty = Random.nextInt(70, 101)`
