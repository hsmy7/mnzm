# 修复弟子自动服用仙元丹没有增加修为的问题 - 实现计划

## [x] 任务 1：分析现有自动服用丹药逻辑
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 分析 `autoUseCultivationPills` 方法的当前实现
  - 对比 `usePill` 方法中手动服用修炼丹的逻辑
  - 确定需要修改的具体位置
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `programmatic` TR-1.1: 确认 `autoUseCultivationPills` 方法错误地将修为百分比加成当作了修炼速度加成
  - `programmatic` TR-1.2: 确认 `usePill` 方法中修炼丹的处理逻辑正确计算并增加修为值
- **备注**：重点关注两种服用方式的差异

## [x] 任务 2：修改 autoUseCultivationPills 方法
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 `autoUseCultivationPills` 方法，使其在自动服用仙元丹时正确计算并增加修为值
  - 确保修为增加量的计算方式与手动服用一致
  - 移除错误的修炼速度加成逻辑
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `programmatic` TR-2.1: 验证修改后的方法能够正确计算并增加修为值
  - `programmatic` TR-2.2: 验证修为增加量与手动服用相同
  - `programmatic` TR-2.3: 验证错误的修炼速度加成逻辑已被移除
- **备注**：参考 `usePill` 方法中修炼丹的处理逻辑

## [x] 任务 3：测试修复效果
- **优先级**：P0
- **依赖**：任务 2
- **描述**：
  - 测试弟子自动服用仙元丹时是否正确增加修为
  - 对比自动服用和手动服用的效果是否一致
  - 确保其他类型丹药的自动服用逻辑不受影响
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `programmatic` TR-3.1: 执行自动服用仙元丹的测试场景
  - `programmatic` TR-3.2: 执行手动服用仙元丹的测试场景
  - `programmatic` TR-3.3: 对比两种方式的效果是否一致
  - `programmatic` TR-3.4: 测试其他类型丹药的自动服用逻辑
- **备注**：确保测试覆盖各种场景