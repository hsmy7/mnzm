# Tasks

- [x] Task 1: 修复 `BattleSystem.executeBattle` 方法中的 `BattleMemberData` 创建逻辑
  - [x] SubTask 1.1: 将 `realm = 9` 改为 `realm = combatant.realm`
  - [x] SubTask 1.2: 将 `realmName = ""` 改为 `realmName = combatant.realmName`

# Task Dependencies
- 无依赖关系，直接修复即可
