# feature/game 模块 Robolectric 默认 SDK。
# includeAndroidResources = true 后 Robolectric 读取合并 manifest（targetSdk 35），
# 超出本项目 Robolectric 版本 maxSdk 34，未显式 @Config(sdk) 的测试类初始化失败。
# 类级 @Config(sdk = [...]) 优先于本配置（如 QuantitySelectorFlowTest 显式 34）。
sdk=34
