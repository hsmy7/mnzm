# SR-0 前置侦察批施工卡（存档云唯一重构 · 首批）

> 立卡：2026-09-21（看护 23:55 派发，新建 ZCode 子会话实施；本会话 = 实施会话）。
> 权威依据 = `docs/save-system-refactor-plan-2026-09-21.md`（§0 决策记录 + §4「SR-0 前置侦察批」+ §5 全局验收门；**该文档 untracked，子会话直读工作区，不自行提交**）。
> 编排台账 = `dispatch-ledger.md` SR 系列批次总表。
> 纪律：本会话只做 SR-0 一批；**零产品代码改动**（验证用临时脚本/测试入测试目录或用后清理并说明）；每子项独立 commit、不夹带；不得自行登记 `accepted`。

---

## 0. 定位

SR 全系列（SR-1…SR-7）的前置事实核：四个设计假设（payload 可压进红线 / TapTap 限额允许多档 / archiveName=slot_N 多档代码层可行 / 冲突仲裁可测）+ 一个审计存疑收口（嵌套事务）。
**无产品代码改动**；产出全部是文档 + 测试目录内的台架测试。

**门**：侦察报告入 `docs/`；Go/No-Go 明确（No-Go = payload 无法压到红线内且分 key 不可行 ⇒ 如实写明回用户重拍板）。

## 1. 五项任务与产出

| # | 任务 | 方法 | 产出 | commit |
|---|---|---|---|---|
| T1 | payload 尺寸分布实测 | 构造多游戏年样本档（1/5/20/50 年 × 不同弟子规模），跑 `SaveFacadeImpl.getStateSnapshot` → serialize+LZ4 量字节 | 尺寸-游戏年曲线；云档裁剪决策（战斗日志限条数 / heavy 分 key 是否必要）→ **IN5 红线值建议**（TapTap 单档上限 10MB 减裕量） | 1 笔 |
| T2 | TapTap v4 限额复核 | 官方文档复核：单档上限、每用户多档数量上限、每分钟上传限频精确语义 | **Go/No-Go** + 槽位云化上限建议 | 1 笔 |
| T3 | 云多档语义勘察 | 读 `TapCloudSaveManager.kt` 的 `CloudSaveApi` 抽象（约 :656-688，XDSdk/TapSdk 双实现），确认 `archiveName=slot_N` 多档并存代码层可行性 | SR-3 槽位映射方案确认；真机/沙盒不可测部分**如实登记「待真机/沙盒实测」** | 1 笔 |
| T4 | 双设备冲突剧本设计 | 剧本级测试设计（两实例交替上传/下载 × 本地脏标志组合） | **SR-2 仲裁单测用例清单** | 1 笔 |
| T5 | 嵌套事务存疑收口 | 审计 §15 存疑 1（外层 `Engine:646` + 内层 `WriteOps:55` 两处 `Room.withTransaction` 是否并入同一事务）实跑验证 | 结论以补注方式回写 `docs/save-system-audit-2026-09-21.md` | 1 笔 |

## 2. 交付物

1. 施工卡（本文件）+ 台账 SR-0 行 in_progress 登记 + 监控日志一行；
2. 侦察报告 `docs/sr0-recon-report-2026-09-21.md`（含：尺寸-游戏年曲线、Go/No-Go 明确结论、IN5 红线值建议、SR-2 仲裁单测用例清单、嵌套事务结论）；
3. 台架测试入测试目录（T1 尺寸台架 / T5 嵌套事务验证），不进生产代码；
4. 审计 §15 存疑 1 补注（T5）；
5. 完成报告 `report-SR0-completion-2026-09-21.md` + 台账 SR-0 行登 delivered。

## 3. 红线复述

- SR 方案文档 untracked，不自行提交；
- 零产品代码改动；T1/T5 台架只入测试目录，并说明保留理由；
- 真机/沙盒无法执行的实测项一律如实登记，不得虚报；
- 台账只动 SR 相关行；构建副产物（`atlas-rgba-manifest.json`）与杂项（`.clash-repair/`、`.workbuddy-ai/`）勿混提交；
- `accepted` 归用户/验收轮，本会话最多登 `delivered`。

## 4. 状态

| 任务 | 状态 | commit | 备注 |
|---|---|---|---|
| 施工卡 + 台账 | ✅ 完成 | `d096a05ef` | |
| T5 嵌套事务 | ✅ 完成 | 见本行 commit 列下笔 | **结论：并入同一事务，存疑解除**（4/4 实测全绿 + Room 2.7.0 源码证据；详见 `docs/sr0-recon-report-2026-09-21.md` §5 与审计 §15 补注） |
| T1 payload 实测 | ✅ 完成 | 见完成报告 | **Go**：最大档 ≈0.28MB（上限 2.8%）；IN5 红线建议 2MB；战斗日志维持 1000 封顶、heavy 分 key 无必要；@Transient 发现（AI 弟子不进云档）登记 SR-3 |
| T2 TapTap 限额 | ✅ 完成 | 见完成报告 | **Go**：100 档/100MB 总量、10MB 单档、限频 1 次/分钟（创建+更新共享冷却，最保守口径）、同档禁并发；slot_N 命名合法；6 槽全额可行 |
| T3 云多档语义 | ✅ 完成 | 见完成报告 | **代码层可行**：CloudSaveApi 全名参数化 + AAR 4.10.5 签名核验逐项匹配；单档硬编码 7 处施工面清单交 SR-2/SR-3；6 项待真机如实登记 |
| T4 冲突剧本 | queued | — | |
| 报告汇总 + delivered | queued | — | |
