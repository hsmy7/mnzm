# 非并行项：真机验证批 / 待拍板四项 / 立项项

> 本文件收录**不能与代码批并行派工**的剩余工作。分三类：需硬件的验证批、需**决策**的拍板项、需**立项**的架构项。
> 开批前请先读 [README](README.md) §1 的分类说明。

---

## 一、batch-22：真机（物理设备）验证批

| 项 | 内容 |
|---|---|
| 性质 | QA / 验证批——**不是代码实现批**（唯一代码面改动：补齐 debug 埋点） |
| 依赖 | 物理 Android 设备（上一轮 batch-10 只在 MuMu 模拟器上完成主体验证） |
| 预分配 | handover **§2.53** |
| 与代码批关系 | 与 11–20 **正交可并行**；但与 **batch-21 必须串行**（先采基线，关闭后复测） |

### 1. 原登记残留 10 项（handover §2.39 / §4.1）

| # | 项 | 验证内容 |
|---|---|---|
| A2 | ASTC 缺失机 RGBA 回退 | 低端机（无 ASTC 支持）走 RGBA 图集回退：图集上传成功、淡入时序正常 |
| A4 | 旋屏表面重建 | 横竖屏切换 → surface 销毁/重建、图集不重复拼装、无黑屏窗口 |
| C1 | 偷盗钩子自然触发 + TapDB 上报 | 道德减益前提下的偷盗链 + 埋点实际到达 |
| C3 | S5 战斗任务 | 任务阁进度门控满足后的任务完成结算/奖励入库/幸存者魂力 |
| C4 | S6 秘境全链 | 出发（换岗/gate/Room 清槽）→ 事件选择（战斗战报/休整/遗迹/方向/AI 遭遇）→ 断线续玩 → 体力耗尽/全灭自动结束 → 手动结束结算 |
| C6 | ThermalMonitor 真实热档 | 真实热状态下 12/6/3 批量上界切换对 AI 修炼演化/兽战的影响 |
| D2 | 放置确认步 | 建筑放置确认流程 |
| D3 | 道路装配 | 道路邻接形态在真机的视觉正确性 |
| E2 | 云存档 | 上传/下载/查询/冲突（**注意：设备上有用户真实云存档时保护性跳过**） |
| E3 | WS-1 绝对值 | 反向信封体积/耗时的真机绝对值——**前置：先补 debug 埋点（见 §3）** |

### 2. 本轮新增（W2-a 及 11–20 的 native 臂）

| 项 | 验证内容 |
|---|---|
| N1 | 库存出售/上架/材料消耗 native 臂（W2-a，1520–1525）：单类出售/批量出售/商人收购/上架撤下/材料消耗在真机 AUTHORITATIVE 下的**真实转发路径**（JVM 单测只覆盖降级臂） |
| N2 | 落档检查：`RNG 通道跨线程进入` 零出现（§2.6 过渡护栏已升级为断言，P1-4 已收口——复测确认无回归） |
| N3 | 各新批（11–20）native 臂的真机回归（每批交付时由其作者登记验证要点，汇总到此） |
| N4 | 存档往返逐字段一致（含新下沉域写入后的存/读往返） |
| N5 | batch-21 关闭后：零数据丢失 + 反向信封体积下降曲线（**与 batch-21 串行**） |

### 3. 前置小改（唯一代码面）

- **补 debug 埋点**：反向信封体积/耗时（`StateSyncService`）与每旬镜像成本（`GameEngineCore`）——仅在 debug 构建输出，release 零开销。
  该小改**可独立开一个小批**（`batch-22a`，§2.53a），不阻塞 11–20。

### 4. 交付物

`docs/parallel-batches/batch-10-verification-record.md` 同格式的**验证记录文档**：逐项 logcat 全量落盘 / 截图 / 构建产物字符串核验 / 结论（通过 / 未达 / 保护性跳过）。
**证据目录不入库**（上一轮 1.5GB 二进制已清理；只留结论文档 + 关键截图）。

---

## 二、待拍板四项（**决策项，未拍板前禁止派工**）

> 这四项都不是"实现工作量"问题，而是"要不要做/接受什么代价"的问题。**未拍板就派工 = 白做**。

### P1. WS-4 NPC 移动系统

| 项 | 内容 |
|---|---|
| 现状 | E3 NPC 纯组件族已就绪（`ecs/npc_component.h`：NpcId/NpcPosition/NpcVelocity/NpcPath/NpcPathIndex/NpcSpriteId/NpcAnimState + 渲染快照通道）；寻路三要素（静态地形 `terrain.h` + 建筑占位 + 道路）已在 C++ 齐备 |
| **缺什么** | **NPC 玩法设计文档**：① 数量上限 ② 生成规则 ③ 与弟子系统的关系 ④ **可行走语义（树/边界是否阻塞、道路是否加速）** |
| 拍板后动作 | 立项 → 设计文档 → 分阶段实现（移动/寻路/渲染/与现有系统交互） |
| 影响面 | 渲染（Vulkan + Canvas 双路径）、ECS、存档协议（NPC 是否入档）、性能（低端机上界） |

### P2. P1-5 月结配对结构级优化

| 项 | 内容 |
|---|---|
| 现状 | 常系数已优化（RNG 引用提升 + 位图判定，每对 O(1)）；**M×F 循环形状 = RNG 消费序**（Kotlin 逐位对拍红线） |
| 决策点 | 是否接受**改变配对行为基线**？结构级降复杂度（如按条件预筛候选集）必然改变 RNG 消费序列 → 双端同步改算法 + 重锚所有既有对拍场景 |
| 若同意 | 双端同步修改 + 新的黄金用例锁定 + 对拍场景全量重跑 |
| 若不同意 | 维持现状（**中等规模存档已达标**，仅大规模存档需优化） |

### P3. 地图跨版本冻结协议

| 项 | 内容 |
|---|---|
| 现状 | 地形不入存档/镜像协议（`terrain.h` 确定性重生 + Kotlin 按种子缓存）——**存档与每旬同步零负担** |
| 决策点 | 是否需要"地图跨版本冻结"语义（即：游戏版本升级后，旧种子的地图**保持不变**）？ |
| 代价 | 入 ProtoBuf + C++ 镜像域：存档 +~30KB、每次 exportState/镜像同步 +~80KB JSON（**WS-1 刚优化掉的持续代价会回来**） |
| 若不需要 | 维持现状（与迁移前 Kotlin 版行为一致：生成器演进会改变旧种子地图） |

### P4. 死代码删除（`TimeSystem.onPhaseTick` / `GameSettingsData.autoSave`）

| 项 | 内容 |
|---|---|
| 现状 | **已拍板保留**（handover §4.2）：前者是 6 个 Diff 测试文件的 Kotlin 跨语言对拍基准；后者是序列化设置字段（删除需改 schema + 迁移） |
| 决策点 | 是否推翻该拍板？ |
| 若推翻（前者的代价） | 需先把 6 个对拍测试改写为纯 C++ 断言——**会失去独立 Kotlin 基准**（防"复刻漂移"的最后一道防线） |
| 建议 | **维持保留**（删除的收益远小于失去基准的风险） |

---

## 三、立项项：WS-1 阶段 3 数据导向存储

| 项 | 内容 |
|---|---|
| 性质 | **需单独立项**（设计文档先行），不是派工批次 |
| 目标 | ① 列级 delta / 二进制通道（替代全量实体 JSON 序列化）；② `dirty_tracker` 列级写屏障 |
| 为什么现在不做 | 涉及 **145+ 列写点**的回归面；协议形状变更会影响全部 46 个 Diff 对拍场景与存档格式 |
| 现状残留口径 | 每旬弟子**全脏**场景的镜像成本受"全量实体 JSON 序列化"支配（协议形状决定，非通道实现可消）；"2x 速每旬非 nativeLoopFrame <2ms" 仅在中小规模存档（≤100 弟子）达成 |
| 立项产出 | 设计文档（协议 v2：列级增量/二进制编码 + 版本协商 + 前后兼容 + 回滚方案）→ 评审 → 分批实施 |
| 与 batch-21 关系 | **batch-21 不依赖本项**（关闭反向通道 ≠ 换前向协议）；本项是独立的性能/协议演进 |

---

## 四、建议的排期（供参考）

```
第一波（可立即并行，6 组）
  组A: batch-11 库存收官 │ batch-12 巡逻住所 │ batch-13 探索
  组B: batch-14 弟子生命周期 │ batch-15 弟子任命 │ batch-16 招募俘虏
  组C: batch-17 生产灵田 │ batch-18 月年边界 │ batch-19 玉符兑换宗门
  组D: batch-20 秘境平台段与战斗执法
  —— 同时: batch-22a debug 埋点小批（不碰上述文件）
第二波
  batch-21 反向通道关闭（等第一波全部合入 main）
  batch-22 真机验证批（与第一波正交；与 batch-21 串行）
决策线（任何时间）
  P1~P4 拍板 → 拍板后 P1 立项；WS-1 阶段 3 立项
```

**给协调人的三条硬提醒**：
1. `GameEngineCoordination.kt` 默认归 batch-18——**它是本轮最大冲突源**，开工前在工作群声明。
2. `GameStateStoreImpl.kt` / `StateSyncService.kt` **只有 batch-21 能动**——任何 11–20 的批想动它，一律驳回。
3. 每批**必须**按 [README](README.md) §3.1 的原子变更集提交共享文件；**冲突不得取 "theirs" 整体覆盖**（上一轮真实事故）。
