# 弟子筛选多选机制改造报告

## 基本信息

| 项目 | 内容 |
|------|------|
| 版本 | v2.3.25 |
| 版本号 | 2038 |
| 日期 | 2026-04-23 |
| 类型 | 功能改进 |

## 需求概述

将弟子界面和选择弟子界面的灵根筛选按钮、属性筛选按钮、境界筛选按钮改为可多选机制，并增加金色高光效果。

### 具体需求

1. **多选机制**：用户可以直接筛选多个属性、境界、灵根的弟子
2. **金色高光**：点击筛选按钮时该按钮金色高光，再次点击则取消筛选高光也消失
3. **固定文字**：筛选灵根或属性时按钮的文字不再变化，固定显示"灵根"和"属性"，避免因为多选多个属性导致文字超出按钮框
4. **下拉按钮高光**：灵根和属性下拉按钮在有子选项被选中时也要高光

## 技术方案

### 核心变更

#### 1. 筛选状态类型变更

| 筛选类型 | 原类型 | 新类型 |
|----------|--------|--------|
| 灵根筛选 | `Int?` | `Set<Int>` |
| 属性筛选 | `String?` | `Set<String>` |
| 境界筛选 | `Int?` | `Set<Int>` |

#### 2. 核心函数签名变更

```kotlin
// 改造前
internal fun List<DiscipleAggregate>.applyFilters(
    realmFilter: Int?,
    spiritRootFilter: Int?,
    attributeSort: String?,
    defaultSortAttribute: String? = null
): List<DiscipleAggregate>

// 改造后
internal fun List<DiscipleAggregate>.applyFilters(
    realmFilter: Set<Int>,
    spiritRootFilter: Set<Int>,
    attributeSort: Set<String>,
    defaultSortAttribute: String? = null
): List<DiscipleAggregate>
```

#### 3. 筛选逻辑变更

```kotlin
// 改造前
val realmFiltered = if (realmFilter != null) sorted.filter { it.realm == realmFilter } else sorted
return if (spiritRootFilter != null) realmFiltered.filter { it.getSpiritRootCount() == spiritRootFilter } else realmFiltered

// 改造后
val realmFiltered = if (realmFilter.isNotEmpty()) sorted.filter { it.realm in realmFilter } else sorted
return if (spiritRootFilter.isNotEmpty()) realmFiltered.filter { it.getSpiritRootCount() in spiritRootFilter } else realmFiltered
```

#### 4. 组件签名变更

**SpiritRootAttributeFilterBar**

```kotlin
// 改造前
internal fun SpiritRootAttributeFilterBar(
    selectedSpiritRootFilter: Int?,
    selectedAttributeSort: String?,
    ...
    onSpiritRootFilterSelected: (Int?) -> Unit,
    onAttributeSortSelected: (String?) -> Unit,
    ...
)

// 改造后
internal fun SpiritRootAttributeFilterBar(
    selectedSpiritRootFilter: Set<Int>,
    selectedAttributeSort: Set<String>,
    ...
    onSpiritRootFilterSelected: (Int) -> Unit,
    onSpiritRootFilterRemoved: (Int) -> Unit,
    onAttributeSortSelected: (String) -> Unit,
    onAttributeSortRemoved: (String) -> Unit,
    ...
)
```

**RealmFilterBar**

```kotlin
// 改造前
private fun RealmFilterBar(
    filters: List<Pair<Int, String>>,
    realmCounts: Map<Int, Int>,
    selectedFilter: Int?,
    onFilterSelected: (Int?) -> Unit
)

// 改造后
private fun RealmFilterBar(
    filters: List<Pair<Int, String>>,
    realmCounts: Map<Int, Int>,
    selectedFilter: Set<Int>,
    onFilterSelected: (Int) -> Unit,
    onFilterRemoved: (Int) -> Unit
)
```

#### 5. 金色高光效果

| 元素 | 未选中 | 已选中 |
|------|--------|--------|
| 背景 | `GameColors.PageBackground` | `GameColors.Gold.copy(alpha = 0.3f)` |
| 边框 | `GameColors.Border` | `GameColors.Gold` |
| 文字颜色 | `Color.Black` | `GameColors.GoldDark` |

## 修改文件清单

### 核心组件定义

| 文件 | 说明 |
|------|------|
| `MainGameScreen.kt` | 定义 applyFilters、SpiritRootAttributeFilterBar、RealmFilterBar、FilterChip、DropdownFilterButton |

### 调用方修改（9个文件）

| 文件 | 修改的弹窗/界面 |
|------|----------------|
| `MainGameScreen.kt` | DisciplesTab、DirectDiscipleSelectionDialog、DispatchTeamDialog、ElderDiscipleSelectionDialog、DiscipleSelectForRewardDialog、CaveDiscipleSelectionDialog、BattleTeamDiscipleSelectionDialog |
| `HerbGardenScreen.kt` | HerbGardenElderSelectionDialog、HerbGardenDirectDiscipleSelectionDialog |
| `ProductionComponents.kt` | ProductionElderSelectionDialog、ProductionReserveDiscipleDialog、RealmFilterRow |
| `PeakScreenComponents.kt` | PeakDiscipleSelectionDialog |
| `LawEnforcementHallScreen.kt` | LawEnforcementDiscipleSelectionDialog |
| `MissionHallScreen.kt` | MissionDiscipleSelectionDialog |
| `LibraryScreen.kt` | LibraryDiscipleSelectionDialog |
| `AllianceDialog.kt` | EnvoyDiscipleSelectDialog、ScoutDiscipleSelectDialog、ScoutRealmFilterBar、ScoutFilterChip |
| `SpiritMineScreen.kt` | SpiritMineDeaconSelectionDialog |

## 代码变更统计

- **修改文件数**：9个
- **涉及调用点**：约 20+ 个弟子选择弹窗/界面
- **核心组件变更**：5个（applyFilters、SpiritRootAttributeFilterBar、RealmFilterBar、FilterChip、DropdownFilterButton）

## 兼容性说明

- 无数据库迁移需求（纯 UI 逻辑变更）
- 无旧存档兼容问题（筛选状态为运行时内存状态，不持久化）
- 属性多选时排序逻辑：取第一个选中的属性作为排序依据（`attributeSort.firstOrNull()`）

## 构建验证

```
BUILD SUCCESSFUL in 3s
45 actionable tasks: 45 up-to-date
```

## 版本信息

- versionCode: 2038
- versionName: 2.3.25
- 已推送至远程仓库
